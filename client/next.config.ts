import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['svc-6f3803a9-morphvm-n7228cap.http.cloud.morph.so'],
};

export default nextConfig;
